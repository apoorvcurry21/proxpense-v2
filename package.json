{
  "name": "proxpense-v2",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspaces",
    "start:api": "npm run start:dev --workspace=api-gateway",
    "start:transaction": "npm run start:dev --workspace=transaction-service",
    "start:wallet": "npm run start:dev --workspace=wallet-service"
  }
}
